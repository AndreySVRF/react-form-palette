import React from 'react';

import Palette from '../../components/Palette/Palette';

const PagePalette = () => {
  return (
    <div className="page-palette">
      <Palette />
    </div>
  );
};

export default PagePalette;
